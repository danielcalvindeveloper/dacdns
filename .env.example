# Configuración MQTT - Copiar este archivo a .env y completar con tus valores
MQTT_BROKER=98f1261f32ac495eb8a03d003a78b5b2.s1.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USERNAME=tu_usuario_aqui
MQTT_PASSWORD=tu_contraseña_aqui

# Intervalo en segundos entre publicaciones del agente (dacdns.py)
# Ejemplo: 1800 = 30 minutos
# Ajustado para entornos con UPDATE_INTERVAL largo
UPDATE_INTERVAL=1800

# Opciones del Cliente VPN (cliente_vpn.py)
# Segundos entre intentos de reescritura del archivo hosts (agrupamiento)
# Reduce escrituras en disco. Elegido menor que UPDATE_INTERVAL pero suficientemente
# grande para evitar escrituras muy frecuentes. Valor recomendado: 600 (10 minutos).
HOSTS_UPDATE_INTERVAL=600

# Minutos para considerar un host inactivo (TTL)
# Debe ser mayor que UPDATE_INTERVAL en minutos para evitar caducidad prematura.
# Con UPDATE_INTERVAL=1800 (30 min) colocamos 60 minutos por coherencia.
HOST_TTL_MINUTES=60

# Si TRUE, el cliente VPN ignorará el hostname local (evita auto-entradas)
# Valores: true | false
IGNORE_LOCAL_HOST=true

# Path opcional al archivo hosts (por defecto Windows)
# HOSTS_FILE_PATH=C:\Windows\System32\drivers\etc\hosts

# Nivel de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
